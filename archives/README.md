# Archived iterations

These folders retain earlier work so the repository records the development journey. They are not the recommended entry points and may require cleanup before being run.

- `rule_based_v3`: VWAP reversion and first execution components.
- `rule_based_v10`: later VWAP/gap-momentum variant with paper-trading code.
- `early_ml_pipeline`: earlier LightGBM and meta-model exploration.
